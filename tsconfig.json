// tsconfig.json (Root) - FINAL CORRECTED PATHS
{
    "files": [],
    "references": [
        // 1. Reference the backend configuration file now located at the root
        {
            "path": "./tsconfig.json"
        },
        // 2. Reference the web configuration file (this path remains correct)
        {
            "path": "./web/tsconfig.json"
        }
    ]
}