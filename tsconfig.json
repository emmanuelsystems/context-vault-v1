// tsconfig.json (Root) - FINAL CORRECTED PATHS
{
    "files": [],
    "references": [
        // 2. Reference the frontend application config file inside web/
        {
            "path": "./web/tsconfig.app.json"
        }
    ]
}