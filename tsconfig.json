// tsconfig.json (Root) - FINAL CORRECTED VERSION
{
    "files": [],
    "references": [
        // 1. Reference the new dedicated backend config file
        {
            "path": "./tsconfig.build.json"
        },
        // 2. Reference the frontend application config file
        {
            "path": "./web/tsconfig.app.json"
        }
    ]
}